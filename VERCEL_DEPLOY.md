# 🚀 Деплой веб-версии на Vercel

## ✅ Готово!

Проект настроен для деплоя на Vercel. Собран production build в папке `dist/`.

## 🎯 Способ 1: Через веб-интерфейс (РЕКОМЕНДУЕТСЯ)

Это самый простой способ!

### Шаги:

1. **Откройте** https://vercel.com/
2. **Войдите** через GitHub
3. **Нажмите** "Add New" → "Project"
4. **Выберите** репозиторий `vk_guests`
5. **Vercel автоматически обнаружит** настройки из `vercel.json`
6. **Нажмите** "Deploy"

**Готово!** Через 1-2 минуты ваше приложение будет доступно по ссылке типа:
`https://vk-guests-xxx.vercel.app`

---

## 🖥️ Способ 2: Через CLI (если нужен контроль)

### Шаг 1: Установите Vercel CLI

```powershell
npm install -g vercel
```

### Шаг 2: Войдите в Vercel

```powershell
vercel login
```

Откроется браузер для авторизации.

### Шаг 3: Задеплойте проект

```powershell
# Тестовый деплой
vercel

# Production деплой
vercel --prod
```

---

## ⚙️ Что уже настроено

✅ `vercel.json` - конфигурация для Vercel  
✅ `app.json` - веб-настройки для Expo  
✅ `dist/` - собранная веб-версия  
✅ Build команда: `npm run build:web`  
✅ Роутинг настроен для SPA  

---

## 🔧 Переменные окружения (опционально)

Если вам нужно добавить переменные окружения:

1. Перейдите в настройки проекта на Vercel
2. Settings → Environment Variables
3. Добавьте нужные переменные

---

## 📦 Структура после деплоя

```
dist/
├── index.html          # Главная страница
├── assets/             # Статические файлы
├── _expo/              # Expo метаданные
└── metadata.json       # Информация о сборке
```

---

## 🐛 Troubleshooting

### Проблема: Белый экран после деплоя

**Решение:**
1. Проверьте Console в браузере (F12)
2. Убедитесь, что VK OAuth настроен правильно
3. Добавьте домен Vercel в разрешенные домены VK приложения

### Проблема: 404 на роутах

**Решение:**
Уже настроено в `vercel.json`:
```json
"rewrites": [{"source": "/(.*)", "destination": "/index.html"}]
```

### Проблема: Ошибка сборки

**Решение:**
```powershell
# Пересоберите локально
npm run build:web

# Проверьте логи
```

---

## 🔗 Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Docs](https://vercel.com/docs)
- [Expo Web Docs](https://docs.expo.dev/guides/customizing-webpack/)

---

## 📱 После деплоя

1. Скопируйте URL вашего приложения (например: `https://vk-guests-xxx.vercel.app`)
2. Добавьте этот домен в настройки VK приложения:
   - Перейдите на https://vk.com/apps?act=manage
   - Выберите ваше приложение
   - Settings → Authorized redirect URI
   - Добавьте: `https://vk-guests-xxx.vercel.app/auth/callback`

---

**🎉 Готово! Ваше приложение доступно в интернете!**
